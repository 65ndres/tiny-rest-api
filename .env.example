# Copy to .env and fill in values before running the app locally.
DEVISE_JWT_SECRET_KEY=change_me_to_a_long_random_string
SUPPORT_USER_PASSWORD=change_me_for_db_seed

# Optional (required for RevenueCat webhooks / Tarpon)
# TARPON_PUBLIC_KEY=
# TARPON_SECRET_KEY=

# Optional (required for password reset / signup verification emails)
# SENDGRID_API_KEY=
# SENDGRID_FROM_EMAIL=tinyrest.app.support@gmail.com
